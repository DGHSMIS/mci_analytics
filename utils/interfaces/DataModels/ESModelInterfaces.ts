export interface ESDateRangeSingleItemQueryInterface {
  from: string; // ISO Date String
  to: string; // ISO Date String
  key: string; // The key to be used in the response object (Date String)
}

export interface ESDateRangeQueryInterface {
  from: string;
  to: string;
  key: string;
}
